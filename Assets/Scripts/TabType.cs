namespace ClockApplication
{
    /// <summary>
    /// An enum for the different tabs in the clock app
    /// </summary>
    public enum TabType
    {
        ClockTab,
        StopwatchTab,
        TimerTab
    }
}